<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">   
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">    
    <title>{{{appName}}}</title>
    <!-- <link rel="stylesheet" href="/css/styles.css">  Link to external CSS file -->
    <style>
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #6D459A, #7721D6, #AA436F, #C53998);
            background-size: cover; /* Static background */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            background: white;
            border-radius: 28px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 950px;
            height: 500px;
            display: flex; /* Flex container */
            overflow: hidden; /* Ensure child elements respect border radius */
        }
        .image-section {
            flex: 2; /* Takes 2/3 of the container */
        }
        .image-section img {
            width: 100%;
            height: 500px;
        }
        .login-section {
            flex: 1; /* Takes 1/3 of the container */
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            text-align: center; /* Ensures text and elements are centered */
            margin-top: 100px;
        }
        .login-section h2 {
            text-align: center;
        }
        .error {
            color: red;
            text-align: center;
        }
        .btn {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 10px 20px;
            background-color: white;
            color: black;
            border: 1px solid #ddd;
            border-radius: 5px;
            text-decoration: none;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: box-shadow 0.3s ease;
            font-size: 14px;
        }
        .btn img {
            margin-right: 10px;
        }
        .btn:hover {
            box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15);
        }
        p {
            margin-top: 120px;
            font-size: 10px;
            padding: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="image-section">
            <img src="images/blog.png" alt="Blog Image">
        </div>
        <div class="login-section">
            <div>
                <img src="images/ibloglogo.png" alt="Google Logo" width="80" height="80">
            </div>
            <h2>Log in to iBlog</h2>
            <!-- Display login error if exists -->
            {{#if loginError}}
                <p class="error">{{loginError}}</p>
            {{/if}}
            <a href="/auth/google" class="btn btn-primary">
                <img src="images/google.png" alt="Google Logo" width="20" height="20">
                Continue with Google
            </a>
            <p>
                By continuing, you agree to iBlog's Terms of Service and confirm that you have read iBlog's Privacy Policy.
            </p>
        </div>
    </div>
</body>
</html>
